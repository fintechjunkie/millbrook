# The old school under occupation

Use `OLD_SCHOOL_B_CANONICAL.png` as the required location reference wherever the camp appears. Take the site from it and nothing else: the building, the fence line, the layout and the paper construction. Camera angle, staging, figures and time of day belong to the individual plate.

`{{LOC:OLD_SCHOOL_B}}`. Locked 2026-07-31; see section 6 of `PATCH_NOTES_FLIPBOOK_ROSTER.md`.

**State B of a pair.** State A is the delivered plate `vol3-s04.png` — the same school in The Patch, boarded and empty behind a sagging chain link fence. The building matches it and must keep matching it. The framing deliberately does not, because state A is shot from outside at ground level and can never show what is now inside the fence.

**The dumpster pad is inside the fence.** The fence runs the outer edge of the concrete and turns around it, so the boiler hatch lying flush in the slab is on the protected side. That is the break-in route in Volume 3 and the reference holds it correctly.

**KNOWN GAP: the tarp-covered object is missing.** Owen counts *“one command tent, four squad tents, a generator, and something under a tarp that isn’t a generator.”* The generator is present on its skid; the tarp is not. Any plate showing the tent line must ADD a low bulky tarp-covered shape near the generator, roped down and not identifiable. Regenerate this reference if that proves unreliable.
